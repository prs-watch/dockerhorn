# dockerhorn-frontend

`dockerhorn` frontend project.

## For Developers

Like `Nuxt.js` project, you can build and run `dockerhorn` frontend.

```bash
# build
$ npm run build
# run as dev mode
$ npm run dev
# run as product mode
$ npm run start
```